/**
 * Example Local Configuration for Loops API Testing
 *
 * This is a template file showing the expected configuration format.
 *
 * SETUP INSTRUCTIONS:
 * 1. Copy this file to 'config.local.js' in the same directory
 * 2. Get your API key from https://app.loops.so/settings?page=api
 * 3. Replace 'your-loops-api-key-here' with your actual API key
 * 4. Save config.local.js (it will be git-ignored automatically)
 *
 * SECURITY WARNING:
 * - config.local.js is git-ignored and should NEVER be committed
 * - This setup is for LOCAL TESTING ONLY
 * - Never expose API keys in production client-side code
 * - For production, implement a backend API to securely handle Loops requests
 */

// Replace with your actual Loops API key
window.LOOPS_API_KEY = '7e6147a29bdc8cdd1221006acd6afc34';
